# 当前执行包验证记录

日期：2026-09-11。本文件仅记录本次交付包的验证，不代表网站已经迁移。

已完成：

- 新增文件前已执行 `git pull --no-rebase`，返回 Already up to date；未执行 push。
- 对合成数据库运行备份工具，源数据库保持 WAL 模式，包含尚未 checkpoint 的已提交记录。
- 导出的独立数据库包含全部测试记录及 70,000 字节 BLOB；导出后源 .db / WAL 的 SHA-256 保持不变。
- 输出目录权限为 0700，数据库为 0600；输出使用 DELETE 日志模式，可作为独立文件传输。
- 备份包 SHA-256、大小、完整性、表计数和外键统计复核成功。
- 现有备份目录拒绝覆盖；缺失源文件不会被误建为空数据库；篡改后的包会被拒绝。
- Compose YAML 已用 PyYAML 解析，并核对仅回环映射应用端口、显式数据挂载、无自动构建、ready 检查和单 worker 配置。
- Markdown 代码围栏和仓库变更范围已检查。

可重复运行的测试（只使用系统临时目录中的合成数据）：

```bash
python3 -m unittest discover -s docs/cloud-migration/scripts -p 'test_*.py' -v
```

尚未执行：

- 本机没有 Docker/Caddy 可执行文件，因此没有运行 Docker 构建、Compose 的真实配置校验或 Caddy validate；YAML 可解析不等于容器可部署。
- 未实现生产模式、密钥迁移、限流和 ready 路由等应用补丁；它们是 README 阶段 B 的后续任务。
- 未导出或转换真实数据库，没有读取邮箱正文或解密真实邮箱密码。
- 未购买云资源、修改 DNS、提交备案、停止旧服务或启动新服务。
- 未进行 30 人负载测试、正式域名验收、对象存储备份或恢复演练。

接手 agent 必须完成这些后续项目，并将“已准备模板”和“已完成部署”分别记录。
