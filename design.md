# 科研协作工作台 · 设计规范

本分支基于 `terra-style` 的业务功能，按 Forecast Center 参考重做视觉，产品名称为科研协作工作台。新增页面或修改 UI 前必须先读本文件；所有页面共用 `frontend/src/index.css` 中的设计变量，不能为新页面另起一套主题。

参考：https://motionsites.org/zh/prompts/forecast-center?copy=1 ，以及用户提供的天气仪表盘截图。借鉴其全屏自然影像、窄侧栏、大号白字、右侧玻璃卡片与宽松间距；业务仍是文献推荐、组会、学术报告和教材管理，不是天气预报服务。

## 1. 配色

| 变量 | 色值 | 用途 |
|---|---|---|
| `--bg` | `#081F28` | 深青主背景、素材未加载时底色 |
| `--panel` | `rgba(22,48,58,.82)` | 文献、组会、教材等阅读卡片 |
| `--surface` | `rgba(9,32,41,.72)` | 输入框、内嵌区域、筛选栏 |
| `--raised` | `rgba(208,231,232,.13)` | 次按钮、选中态、悬停态 |
| `--glass` | `rgba(119,150,151,.24)` | 首页右侧透景卡片 |
| `--text` | `#F5F8F6` | 主要文字 |
| `--soft` | `#D9E4E2` | 正文、次要说明 |
| `--muted` | `#B2C6C8` | 日期、作者、次要元数据 |
| `--accent` | `#C5E6DF` | 主按钮、链接、选中标记 |
| `--accent-strong` | `#E4F4EE` | 主按钮悬停 |
| `--accent-ink` | `#102F33` | 浅色主按钮上的深色文字 |
| `--line` | `rgba(218,238,235,.19)` | 分隔线与默认边框 |
| `--warning` | `#F3D8A2` | 台内报告、定向推荐标记 |
| `--warning-bg` | `rgba(181,140,76,.17)` | 警示、报告区域背景 |
| `--danger` | `#FFC2C4` | 错误、危险操作文字 |
| `--danger-bg` | `#4B2932` | 错误提示背景 |
| `--success` | `#AFE2C4` | 已完成、成功状态 |
| `--focus` | `#D0ECE2` | 键盘焦点轮廓 |

首页保留背景纹理：左侧深青渐变遮罩，底部加深以保护日程文字。业务内页使用 `rgba(5,25,34,.82)` 遮罩，以阅读和表单对比度为先。原始云景里的金色只作为背景辅助色，不把亮黄色用作主按钮。

## 2. 字体与层级

统一 `Inter, -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif`。不混用宋体论文标题与无衬线导航。数字使用 `font-variant-numeric: tabular-nums`，不依赖强制等宽字体。

| 层级 | 桌面字号 | 字重 | 行高 |
|---|---|---|---|
| 首页 H1 | `clamp(38px,4.9vw,70px)` | 400 | 1.28 |
| 内页 H1 | `clamp(28px,3.2vw,40px)` | 400 | 1.35 |
| 组会日期大数字 | 88px | 300 | 1 |
| 日程日号 | 32px；手机 26px | 300 | 1.3 |
| 模块 / 论文标题 | 20–21px | 500 | 1.5 |
| 正文 | 15–16px | 400 | 1.65–1.8 |
| 常用按钮、表单标签 | 14px | 400–500 | 1.4–1.65 |
| 辅助元数据 | 12–13px | 400 | 1.5 |
| 英文眉题 | 12px，字距 .13em | 500 | 1.5 |

手机首页 H1 使用 `clamp(32px,8.3vw,46px)`。10–11px 仅用于手机侧栏短标签、日期状态与装饰眉题，不用于正文、输入或主要操作。长标题换行，不能溢出卡片；避免全段加粗。

## 3. 间距与布局

- 基础间距：4 / 8 / 12 / 16 / 20 / 24 / 32 / 44 / 48 / 56 / 72px。
- 桌面固定侧栏宽 88px；图标热区 48×48px，纵向间距 12px；活动项有左侧 3px 标记。
- 内容左右留白 40px；1100px 以下 28px；650px 以下 16–18px。
- 首页主体：`minmax(0,1fr) 288px` 两列，间距 40px；1600px 以上右栏 320px、列间距 56px。右侧最近组会卡片按内容高度展示，不强制拉伸到左栏高度；快捷卡片最小高度 78px，避免首屏被大面积卡片挤出视口。
- 首页左侧：任务引导、组会 CTA、文献推荐与文献库快捷入口、周日程；右侧：最近组会、真实天气和教材资料。真实数据直接可见，不能另加营销页挡在工作台前。
- 首页 900px 以下改为单列；右侧组会占一整行。650px 以下所有卡片单列。
- 650px 以下侧栏变成固定 65px 顶栏，展示五个短导航标签与头像，收藏、设置和退出收纳到头像菜单；正文预留顶栏空间。
- 业务内页最大宽 1440px，顶部 48px、底部 64px；卡片内边距 24–26px，手机 18–20px。
- 日历可在自身容器中横向滚动，整页不得产生水平溢出。首页七天卡片保持一排。
- 主要按钮间距 12px；表单控件纵向 16px；模块间距 28–48px。

## 4. 组件规则

### 按钮与导航
- 默认按钮圆角 12px、最小高 44px、内边距 10px 18px、1px 边框；小按钮最小高 36px。
- 主按钮：`--accent` 底、`--accent-ink` 字；悬停 `--accent-strong`，不要蓝色霓虹或厚阴影。
- 次按钮：`--raised` 底、`--text` 字、`--line` 边框；文字按钮透明。
- 图标按钮 40×40px、圆角 12px；必须有 `aria-label`。
- 侧栏背景 `rgba(66,94,102,.28)`、模糊 24px、右侧 1px 分隔线；保持常驻，不依赖鼠标移到窗口边缘才能发现导航。
- 筛选分段圆角 12px，内部选项圆角 8px，选中使用 `--raised`；不得仅靠颜色表达选中状态。

### 卡片与弹窗
- 标准卡片圆角 20px、1px `--line` 边框、`0 12px 40px rgba(1,15,21,.14)` 阴影；首页透景卡片模糊 24px。
- 阅读卡片背景比首页更实，正文必须可辨；不要在每个嵌套区域重复重阴影。
- 弹窗圆角 24px、背景 `#18323B`、最大高随视口滚动；标题栏固定，遮罩 `rgba(0,15,21,.6)` + 8px 模糊。
- 输入圆角 12px、内边距 11px 14px、深色背景，焦点环 2px `--focus`；保留日期、文件、选择器的原有行为。
- 标签使用胶囊圆角，仅承载状态；日程、定向、错误需要文字说明，不只靠颜色。

## 5. 动效与素材

- 本地背景：`frontend/public/assets/forecast/clouds.jpg`，固定静态背景，保留云景，不再播放背景视频。
- 云景由内置 imagegen 生成；视频是基于静态图的缓慢往返缩放，不是真实天气观测或原参考视频。周期首尾位置一致，避免循环硬切。不使用原参考页面自带文字的预览视频当背景。
- 不提供背景播放按钮，不闪电闪屏，不做持续摇晃或卡片 3D 倾斜。
- hover / focus 动效 180–200ms，页面过渡遵循系统减少动态偏好。
- 不支持背景模糊时使用实色 `#28464D` 回退。

## 6. 内容与后续页面要求

- 页面 title：`科研协作工作台`。界面品牌使用 `LabOrbit`。
- 主要 CTA 沿用参考提示词 `Open Forecast Center`，进入真实周日程；标题和说明适配现有科研业务，不照搬与业务无关的英语占位营销文案。
- 统计、下一次组会与周日程从当前用户有权限的现有 API 读取；不能使用随机数字或虚构天气曲线填充界面。
- 加载中、零条记录、失败必须分开显示。权限仍由后端控制，不能为首页摘要额外暴露定向推荐。
- 新页面先读本文件，复用 CSS 变量与现有按钮、弹窗、状态组件；确需新增规范时同步更新本文件。
- 检查桌面和手机的布局、键盘焦点、长标题、空数据、失败状态。主要用户动作必须在窄屏可完成。

## 7. 个人收藏与问题反馈

- 收藏页沿用深蓝绿 `--panel` 毛玻璃卡片，圆角 20px，桌面双列、850px 以下单列，列间距 20px；内边距桌面 24px、手机 18px。
- 标题沿用全站 28–40px 层级，收藏卡片标题 21px（手机 19px）、字重 500、行高 1.45；摘要 14px、行高 1.8，作者与辅助信息 12–13px。
- 收藏星标复用 40px 图标按钮与 Phosphor 星形图标。未收藏为空心，已收藏为实心并使用 `--warning` / `--warning-bg`；提供 tooltip、aria-pressed、键盘焦点、180ms hover / active 反馈。不得改变原有文献标签与 PDF 的位置关系。
- 收藏只保存到当前账号；推荐流与文献库对同一 arXiv 文献共享星标。收藏页提供分类、搜索及独立加载、空白、失败状态，访问范围由后端校验。
- 反馈使用现有 BaseDialog，包含标题、描述与明确提交结果。全体登录成员可提交，提交人可读取自己的反馈与处理回复，管理员可读取全部反馈并管理状态；其他成员不可访问。
- 管理页复用毛玻璃面板与分段筛选，提供全部、待处理、已处理状态。侧栏管理员入口仅对管理员显示。
- 手机底栏保留用户头像，收藏与反馈入口收纳进头像菜单，菜单高度随视口滚动；不隐藏手机上的核心操作。

## 8. 账户、期刊论文与组会提醒

- 移除浏览器分享书签功能，侧栏原书签位置固定为“我的收藏”。手机头像菜单从顶栏下方向下展开，不能被定位到屏幕之外。
- 账户设置页最大宽 900px；头像、个人资料、邮箱密码分为三个标准毛玻璃面板，内边距 24px / 手机 18px。头像预览 88px 圆形，侧栏头像沿用 34–36px；照片等比裁切，不变形。
- 真名和昵称明确区分；邮箱密码修改单独提交并验证当前密码。上传、加载、失败和提交中的状态需要可见，不输出密码或敏感令牌。
- 期刊与 arXiv 共用推荐卡片、收藏和文献库，不新建第二套视觉系统。显示真实期刊名或 DOI，只有存在 PDF 地址时才使用 PDF 按钮文案，否则显示“期刊原文”；保持原 PDF 按钮的位置。
- DOI 自动解析与手动期刊录入并列；手动字段复用 form-grid / form-row，辅助说明 13–14px。长 DOI 和期刊名可折行。
- 初始排期用 CSV 上传并预览，每行明确选择主讲人账号；摘要独立于纪要，初排不必填写。
- 组会前七天的站内待办使用固定提醒按钮，桌面右上、手机右下；点击现有 BaseDialog 填摘要，不用打断式自动弹窗。已填写、取消、改期或重新指派后及时更新，提醒只对对应主讲人显示。

## 背景素材记录

工具：内置 imagegen。生成提示词：

> Use case: photorealistic-natural. Asset type: clean cinematic background plate for a research dashboard, 1920x1080 landscape. A vast dramatic storm cloud shelf above a distant low green countryside horizon. Huge sculpted deep petrol teal blue clouds across upper 80% of frame, warm muted sunlight breaking through far right near horizon, dark soft open space on left for white interface text. Immersive atmospheric photography, highly realistic cloud textures, calm premium editorial color grading, no lightning flashing, no buildings, no people. Low forested rolling hills and fields bottom 15%. NO text, NO interface, NO graphs, NO numbers, NO logos or watermarks. Full bleed wide image.

## 9. 反馈处理、资料上传与个人安排

- 管理员侧栏“反馈管理”直接进入管理页；普通成员侧栏“我的反馈”进入自己的记录。两页共用标准毛玻璃卡片、筛选与搜索，回复区域使用 `--surface`，圆角 12px、内边距 12–16px。
- 管理员回复保留时间、作者、已读状态；提交者主动标记已读。发送操作与处理状态明确区分，可发送进展并保留待处理。
- 资料表单复用 FileField，支持 PDF 上传和外链。上传中禁用保存与关闭；卡片复用 AttachmentLink 以携带认证读取附件。
- 首页“我的汇报安排”展示个人下一次主讲与 arXiv 分享。桌面双列、手机单列，16px 间距、16–20px 内边距，倒计时 32px / 400；按北京时间显示今天或剩余天数。仅显示真实安排，未安排和读取失败分别表达。
- 原有主讲摘要七天提醒继续保留；分享人需关联账号，旧姓名记录只做唯一精确匹配。首页反馈回复入口显示未读数量，每分钟及返回页面时刷新。
